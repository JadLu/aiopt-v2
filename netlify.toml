[build]
  command = "pnpm build"
  publish = ".next"

# Omit Cloudinary public vars from secrets scanning — they're meant to be client-side
[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME,NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET"
